from datetime import datetime

def Add_Into_Archive(archive_filename, keys, values):
    print('in archive')
    file = open(archive_filename, 'a')
    s = keys + '   ' + values + '   ' + datetime.strftime(datetime.now(), "%d.%m.%Y %H:%M:%S")
    file.write(s + '\n' )
    file.close()